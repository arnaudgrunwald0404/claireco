{
  "name": "claireco",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod --dir=."
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
